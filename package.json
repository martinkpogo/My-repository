{
  "name": "enig-agent",
  "version": "1.0.0",
  "private": true,
  "description": "ENIG AI agent runtime: Cloudflare Worker + Workers AI, Telegram as chat/approval interface, Notion as system of record.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "@types/node": "^22.20.2",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
